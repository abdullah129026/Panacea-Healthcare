import { Header } from "@/components/layout/Header";
import { Card, CardTitle, Button, Badge } from "@/components/ui";
import { Save, Download, Sparkles, Activity, HeartPulse, FlaskConical, Stethoscope } from "lucide-react";

const drivers = [
  { label: "Hypertension Management", level: "Critical", pct: 88, tone: "error" as const },
  { label: "Metabolic Syndrome", level: "High", pct: 65, tone: "warning" as const },
  { label: "Genetic / Family Analysis", level: "Moderate", pct: 40, tone: "info" as const },
];

const params = [
  { label: "Blood Pressure", value: "138/84 mmHg" },
  { label: "LDL Cholesterol", value: "165 mg/dL" },
  { label: "HbA1c", value: "6.4%" },
  { label: "BMI", value: "28.7" },
];

const recommendations = [
  { icon: Activity, title: "Optimize ACE-I Inhibitor Dosage", body: "Current BP of 138/84 suggests current regimen is suboptimal. Recommend increasing Lisinopril to 30mg or adding 12.5mg HCTZ." },
  { icon: HeartPulse, title: "Immediate Echo & Stress Test", body: "Due to the significant rise in the score (+15% over 12 months), a baseline echocardiogram is recommended within 14 days." },
  { icon: FlaskConical, title: "Metabolic Referral", body: "HbA1c levels and BMI indicate progression toward Type 2 Diabetes. Schedule dietary consult and endocrine review." },
];

export default function AIAnalysisResultPage() {
  return (
    <div className="flex flex-col">
      <Header breadcrumbs={[{ label: "AI CDS", href: "/ai-cds" }, { label: "Analysis Results" }]} />
      <div className="p-6 space-y-6">
        <div className="flex items-start justify-between">
          <div>
            <h1 className="text-2xl font-bold font-mono text-foreground">Cardiovascular Risk Assessment</h1>
            <p className="text-sm text-muted-foreground mt-1">Generated by AI · Panacea AI Core on Oct 24, 2023 · 14:23 PM</p>
          </div>
          <div className="flex items-center gap-3">
            <Button variant="outline" size="sm"><Save className="w-4 h-4" /> Save</Button>
            <Button variant="outline" size="sm"><Download className="w-4 h-4" /> Export</Button>
            <Button variant="primary" size="sm"><Sparkles className="w-4 h-4" /> Create Treatment Plan</Button>
          </div>
        </div>

        <div className="grid grid-cols-3 gap-6">
          {/* Risk score */}
          <Card className="flex flex-col items-center text-center gap-3">
            <div className="relative w-40 h-40 rounded-full flex items-center justify-center"
                 style={{ background: "conic-gradient(#D93C15 0% 78%, #E5DCDA 78% 100%)" }}>
              <div className="w-32 h-32 rounded-full bg-card flex flex-col items-center justify-center">
                <span className="text-4xl font-bold font-mono text-error-foreground">78%</span>
                <span className="text-[10px] uppercase tracking-wider text-muted-foreground">High Risk</span>
              </div>
            </div>
            <p className="text-xs text-muted-foreground">
              Patient demonstrates clinical indicators placing them in the top 5th percentile of acute risk for major adverse cardiac events (MACE) within 24 months.
            </p>
            <Badge variant="error">Trend ↑ +15%</Badge>
          </Card>

          {/* Top drivers */}
          <Card className="space-y-4">
            <CardTitle>Top Risk Drivers</CardTitle>
            {drivers.map((d) => (
              <div key={d.label} className="space-y-1.5">
                <div className="flex items-center justify-between text-sm">
                  <span className="text-foreground">{d.label}</span>
                  <Badge variant={d.tone}>{d.level}</Badge>
                </div>
                <div className="h-2 rounded-full bg-muted overflow-hidden">
                  <div className="h-full rounded-full bg-primary" style={{ width: `${d.pct}%` }} />
                </div>
              </div>
            ))}
          </Card>

          {/* Patient comparison */}
          <Card className="space-y-3">
            <CardTitle>Patient Comparison</CardTitle>
            <div className="h-32 rounded-xl bg-gradient-to-br from-primary/10 to-info/30 flex items-center justify-center">
              <Stethoscope className="w-8 h-8 text-primary/40" />
            </div>
            <div className="flex items-center justify-center gap-4 text-xs">
              <span className="flex items-center gap-1.5"><span className="w-2.5 h-2.5 rounded-full bg-primary" /> This Patient</span>
              <span className="flex items-center gap-1.5"><span className="w-2.5 h-2.5 rounded-full bg-info-foreground" /> Cohort Median</span>
            </div>
          </Card>
        </div>

        <div className="grid grid-cols-3 gap-6">
          <Card className="space-y-3">
            <CardTitle>Patient Parameters</CardTitle>
            {params.map((p) => (
              <div key={p.label} className="flex items-center justify-between text-sm">
                <span className="text-muted-foreground">{p.label}</span>
                <span className="text-foreground font-medium font-mono">{p.value}</span>
              </div>
            ))}
          </Card>

          <Card className="col-span-2">
            <CardTitle className="flex items-center gap-2 mb-4"><Sparkles className="w-4 h-4 text-primary" /> Clinical AI Recommendations</CardTitle>
            <div className="space-y-3">
              {recommendations.map((r) => (
                <div key={r.title} className="flex gap-3 p-3 rounded-xl border border-border">
                  <div className="w-9 h-9 rounded-xl bg-primary/10 flex items-center justify-center shrink-0">
                    <r.icon className="w-5 h-5 text-primary" />
                  </div>
                  <div>
                    <p className="text-sm font-medium text-foreground">{r.title}</p>
                    <p className="text-xs text-muted-foreground mt-0.5">{r.body}</p>
                  </div>
                </div>
              ))}
            </div>
            <button className="text-xs text-primary hover:underline mt-3">View Evidence &amp; Sources</button>
          </Card>
        </div>
      </div>
    </div>
  );
}
